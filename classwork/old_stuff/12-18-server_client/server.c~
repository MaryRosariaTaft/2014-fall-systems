int main(){

  int from_client;
  int to_client;
  char line[100];

  //well-known pipe
  mkdfifo("mario",0644);
  from_client = opem("mario",O_RDONLY);
  read(from_client,line,sizeof(line));

  //process
  to_client = open(line);

  close(from_client);

}
